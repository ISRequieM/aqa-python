
version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.1

    working_directory: ~/aqa-python

    steps:
      - checkout
      - run:
          name: run tests
          command: |
            python -m pytest phase01/tests/testFibonacci.py

      - store_artifacts:
          path: test-reports
          destination: test-reports
